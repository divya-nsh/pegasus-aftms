# AFTMS

Tech Stack: Pnpm Monorepo wtih TRPC And react with tanstack router

The client imports tRPC types from the server (`import type` + `server/*` path). Typecheck for that setup is documented in [docs/typescript-client-importing-server-types.md](docs/typescript-client-importing-server-types.md).
